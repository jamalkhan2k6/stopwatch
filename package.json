{
  "name": "stop-watch-counter",
  "version": "1.0.0",
  "description": "I needed a stopwatch with a counter for hourly jobs.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . multi-gmail-app --overwrite --asar=true --platform=win32 --arch=ia32 --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"MultiGmail\""
  },
  "repository": "",
  "keywords": [
    "Electron",
    "counter",
    "watch",
    "stop",
    "stop-watch"
  ],
  "author": "Jamal Khan",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^2.0.12",
    "electron-packager": "^12.2.0"
  },
  "dependencies": {
    "electron-store": "^2.0.0",
    "electron-tabs": "^0.9.4",
    "electron-window-state": "^5.0.1",
    "cryptiles": ">=4.1.2"
  }
}
